node_modules/
dist/
build/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.DS_Store
.env
.env.local
*.env
# Local agent/editor configs that may hold API keys — never commit.
opencode.jsonc
.opencode.json
.mcp.json
*.log
coverage/
.vite/
.turbo/
# Cloudflare Worker local state + the built /app console bundle. deploy.sh
# rebuilds site/app from apps/web on every deploy — it's an artifact, not source.
.wrangler/
site/app/
# Anywhere in the tree — the cache lives at apps/api/data/tilecache; the old
# root-anchored pattern missed it and 2.7k PNGs got committed.
**/data/tilecache/
# History playback SQLite store (refills from live data; never commit).
**/data/history.db
**/data/history.db-*
.playwright-mcp/
# Dev screenshots / scratch images at the repo root — keep the tree tidy.
# (Real product art lives in code as SVG, or under docs/ if ever needed.)
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp
apps/ml/fusion/data/
apps/ml/fusion/runs/
apps/ml/fusion/.mamba-cuda/
apps/ml/fusion/EOGS/
apps/ml/fusion/.mamba-root/
apps/ml/fusion/.mamba-colmap/
apps/ml/fusion/maxar/
apps/ml/fusion/3dgs/
apps/ml/fusion/3dgs_data/
apps/ml/fusion/3dgs_data.zip
apps/ml/fusion/drone/
apps/ml/fusion/output_3dgs/
# Orphaned 3D reconstruction tree (~566MB) — COLMAP dense, gsplat checkout,
# render outputs, and large video renders. Never commit these artifacts.
apps/ml/fusion/recon/
*.mp4
apps/ml/fusion/**/colmap/dense/
apps/ml/fusion/**/gsplat_repo/
apps/ml/fusion/**/output/
tmp/
apps/ml/fusion/.recon_jobs/
apps/ml/fusion/.sat_data/
vggt/
mapanything/
brush/
apps/native/target/
apps/web/public/darktiles/
apps/web/public/basemap/*.pmtiles
.perf-backup/

# Generated knowledge-graph artifact (regenerate with /graphify).
graphify-out/

# Research artifacts / scratch — not source. Keep the repo to code + docs.
Palantir_Gotham_*.pdf
Palantir_Gotham_*.docx
deep-research-report.md
sim-running-snapshot.md
eusi_*_b64.txt
# Agent tooling & planning skill output — local workflow, not shipped.
.claude/
docs/superpowers/

# YOLO model weights (auto-downloaded at runtime, never committed)
*.pt
