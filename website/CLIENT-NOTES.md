# CLIENT-NOTES — Velocity marketing site

- 2026-07-24 intake: single-page landing; deploy = Cloudflare static assets (wrangler.toml added, NO deploy without approval); imagery = both real-screenshot composites AND generated atmosphere plates, "palantir website level"; open-source only, all CTAs → https://github.com/AndrewCTF/velocity; no SaaS/pricing.
- Secret: OpenRouter key lives ONLY in website/.env (gitignored). Never commit, never log.
- Reference sampled live 2026-07-25: palantir.com home = photographic full-bleed hero + white type, light #efefef editorial body, giant gray centered statement type, charcoal rgb(30,33,36) squared buttons, 10px uppercase labels, dark closing dual-CTA band, 6,649px tall, Alliance No.1/No.2 grotesque.
- Authenticity: no invented metrics/testimonials/logo walls. All numbers from README / Show HN draft (measured 2026-07-17).
- 2026-07-25 model substitution: user-specified `microsoft/mai-image-2.5-pro` does not exist on OpenRouter (verified via /api/v1/models); used `google/gemini-3-pro-image` (closest text+image→image). 4 plates generated, one lighting world, no trademarks.
- The faint giant "EUROPE" on the hero laptop screen is IN the real product screenshot (basemap region label), not a generation artifact. Do not "fix" it.
- 2026-07-25 judge round (3 independent adversarial panels): copy judge REFUTED (6 findings), parity judge REFUTED narrowly (6), AI-tell judge NOT REFUTED (6 minor). All 16 distinct findings fixed in rounds 3-4 EXCEPT: hero staged-laptop concept kept (explicit client brief) and Geist kept (art-direction choice, hook finding acknowledged). Key fixes: peak-vs-typical numbers reconciled in both places, legal FAQ states the mechanism (local redisplay, no public re-serving), fps claim tied to the measured RTX desktop, evidence section rebuilt as hairline chain strip (AI still-life deleted), gallery aligned to page margin + honest "six of the twelve" copy + README-tour link, second statement band added, single big closing CTA, disk-growth FAQ added (~3 GB/h measured, 2 GB default cap, ARCHIVE_MODE), screenshots brightened +18%.
- Sellability loop: 4 render rounds, final lints green (0 dashes, 0 external hosts, 0 mobile overflow, fonts self-hosted verified via document.fonts).
- 2026-07-25 v1 REJECTED by the client against palantir/l3harris/defense peers. Root cause was structural, not styling: cream base made full-bleed impossible (every product asset is a dark console, so screenshots could only float in bordered boxes), one layout family repeated 5x, and a staged-laptop hero instead of the subject. Measured: v1 was the TALLEST page in the reference set (8,257px) and the least visual (1 image per 2,064px vs Anduril 1/491, Shield AI 1/297, Palantir 1/171).
- v2 direction "the console at altitude": true black `#05070a` sampled from the app's space background, one light band at the archive argument (Palantir's white-as-punctuation grammar), accent `#d946ef` = the product's own selection colour (an enforced invariant in styles.ts), CTAs only. Display 104px / -0.038em. Signature = the hairline data rule from the app's scrubber chrome.
- ALL v2 imagery is real: 5 plates captured at 2560x1440 from the running app (live backend, 11,532 aircraft / 2,867 vessels), chrome hidden via a DOM walk that keeps only the Cesium canvas ancestry. Camera scripted through `window.__viewer` (dev build only; prod has no dev globals). Plates were graded up (brightness 1.10-1.34) because the app renders for a dark ops room and the ungraded plates read as black rectangles at thumbnail scale beside the reference set. The v1 generated desk plates are retired.
- v2 capture traps: Playwright fullPage screenshots LIE about scroll-reveal state (they showed ~4,000px of blank bands that the DOM proved were at opacity 1). Always step the viewport. Also `IntersectionObserver` threshold 0.25 never fires for elements taller than the viewport, which left 3 gallery cards permanently invisible; fixed to threshold 0 + rootMargin.
- v2 verified 2026-07-25: 8,854px desktop / 11,866px mobile, 12 real images (1 per 738px), 4 full-bleed bands, 0 em dashes, 0 horizontal overflow at 390px, 0 external hosts at runtime, 0 hidden reveals, 2 accent fills (both CTAs), fonts self-hosted, count-ups land on exact values, reduced-motion parity clean. NOT deployed: G3 approval outstanding, and `site/public/` still holds the v1 copy.
- 2026-07-25 v2 first pass ALSO called mediocre against the same peers. Correct call, and the cause was the hero subject: I led with a screenshot of the app's dark 2D basemap. A screenshot of a map reads as UI. Anduril leads with a submarine, Shield AI with a lit Earth, L3Harris with gold light. Measured mean luma of the hero at thumbnail scale: Velocity 22 vs Anduril 41, Palantir 46, Shield AI 49. It was literally the black rectangle in the row.
- FIX: the app has a `3d-sat` basemap in the top-bar `<select>` that nothing on the site was using. Switching to it gives blue oceans, orange deserts, cloud, and a lit atmosphere limb against black space. Hero is now `hero-orbit` at lon 52 / lat 22 / h 4.2 Mm / pitch -64 / heading -14, which frames the Mediterranean, Levant, Gulf and Caspian in one shot with several hundred live aircraft on it. Post-fix hero luma 39, inside the reference band.
- The hero is now VIDEO, which all four references have and v2 did not: 72 frames captured while drifting lon 45 to 60, encoded forward+reversed for a seamless 6 s ping-pong loop (webm 1.4 MB / mp4 1.2 MB, poster 432 KB). `prefers-reduced-motion` hides the video and paints the poster as a background instead. Recipe is in scratchpad `cine.mjs`; the ffmpeg filter is `split[a][b];[b]reverse[r];[a][r]concat=n=2`.
- 2026-07-25 client: the ping-pong loop reads as a wobble, wants a true 360. Rebuilt as a real full rotation: 240 frames, lon 45 stepping 1.5 deg, so frame 239 sits exactly one step short of frame 0 and the loop closes with NO reverse concat. 16 fps = a 15 s rotation. mp4 1.9 MB (crf 36) / webm 2.3 MB (crf 50) / poster 374 KB, both verified with `ffmpeg -f null -` (zero decode errors) and 240 frames at 1600x900.
- While shooting the 360 the GDELT event layer turned out to be the real blocker: over CONUS its "ACTOR → ACTOR · event (Nx)" labels stack into a solid wall of grey text that hides the map. Kill it with `window.__registry.disable('conflict.gdelt.live')` (registry ids live in `apps/web/src/registry/defaults.ts`, `__registry` is a DEV global from App.tsx:152). Clicking the layer rail does NOT work here because the rail is collapsed at capture time. With it off you see thousands of individual aircraft glyphs over North America, which is the best frame in the loop. Leave the layer ON for the console/product shots; it is part of the product.
- Encode trap: two `encode-spin.sh` runs raced on the same output path and produced an mp4 full of "Invalid NAL unit size" errors that still probed as a valid 15 s file. Always re-run the encode alone and verify with `ffmpeg -v error -i out.mp4 -f null -` before trusting it.
- Hero contrast must be measured across the WHOLE loop, not one frame: pause the video at each second, hide the type, sample the brightest 2% of the backdrop per text box. Worst case across all 15 s is h1 3.18:1 (at t=0), sub 8.88:1, eyebrow 7.89:1, all passing. A single-frame reading is worthless here and once reported a bogus 12.94:1 because the video had not painted.
- Hero contrast was fixed from MEASURED pixels, not by eye: screenshot the hero with `.hero-inner > *` set to `visibility:hidden`, then sample the brightest 2% of the backdrop inside each text element's real bounding box. First pass measured h1 2.93:1 (needs 3.0) and eyebrow 1.91:1 (needs 4.5). A full-frame scrim fixes contrast by killing the image, so the fix is a local radial scrim under the type column only (`.hero-inner::before`, `right: 22%`) plus lifting the hero eyebrow to `#ccd6e0`. Final: h1 3.28:1, sub 8.80:1, eyebrow 8.15:1, all measured.
- Measurement trap: give the eyebrow `display:inline-block` before sampling. As a block it spanned 1160px, so the sample ran past the scrim into the bright globe and reported 1.86:1 for text that only occupies the first 343px.
- Retired in v2 final: `plate-gulf`, `plate-baltic`, `plate-hero-atlantic`, `hero-singapore-strait` (dark-basemap or thin-data), and all three `gen-*` v1 plates. Singapore has no keyless AIS coverage in the current union, so do not try to re-shoot it.
- 2026-07-25 new logo. The old mark was a line-chart tick with a node, which is the generic SaaS-analytics default and the wrong register for a situation console. Worse, the app shipped a DIFFERENT mark (a teal globe favicon at `apps/web/public/favicon.svg`), so site and product disagreed.
- New mark: the V is a recorded track. An OPEN square is an archived fix, a SOLID square is the live one, and the stroke between them is the history you own, which is the product's actual differentiator. Geometry on a 32 viewBox: `M7 11 16 25.5 25 11` stroke 2.8 with a MITRE join for a crisp vertex, open rect at 3.9/4.9 6.2sq stroke 2, solid rect at 21.9/4.9 6.2sq. Both arms stop at y=11, the squares' lower edge, so the track never enters the open square and its hole survives at 16 px (verified at 8x magnification).
- Rejected in round 1, with the reason, so nobody re-proposes them: an Earth-limb arc plus fix reads as a frown; three stacked arcs read as a christmas tree; a plain solid V and a V-in-a-circle both read fine but carry no idea and anyone could own them.
- Files: `assets/logo.svg` (currentColor, recolour via the parent's `color`), `assets/logo-mark-magenta.svg`, `assets/favicon.svg` and `assets/apple-touch-icon.png` (a REAL 180x180 PNG, because iOS ignores SVG apple-touch-icons), plus `apps/web/public/favicon.svg` so the product matches. Inline copies live in the site nav and footer.
- 2026-07-25 client: "the pink looks so unprofessional". Correct, and the mistake was mine specifically. `#d946ef` is the product's real selection colour, but a 1px track on a dark map and a 52px solid button fill are not the same perceptual object; promoting a functional data colour to a brand fill made it read consumer/crypto. None of palantir, anduril, shield.ai or northrop puts a saturated hue on a button.
- Chrome is now MONOCHROME. Primary CTA is bone `#ecebe7` on `#0d1014` text, which is the anduril/shield.ai move. All colour on the page comes from the imagery (amber aircraft, teal clusters, blue ocean) and none from the chrome. Tested amber `#e3a33a` too and rejected it: it competes with the aircraft glyphs in the same frame and reads as a caution state. Magenta is gone from index.html, styles.css, both favicons, the apple-touch-icon and the app favicon; `logo-mark-magenta.svg` renamed to `logo-mark-light.svg`. Measured: CTA text 15.98:1, fill vs surface 16.91:1.
- 2026-07-25 client: the mobile navbar. It was not a style problem, it was a navigation failure. `.nav-links` was `display:none` below 760px with NO menu behind it, so all five section links were unreachable and the bar had 134px of dead space between the wordmark and the GitHub button. Added a real menu: 44px toggle, panel under the bar, `aria-expanded`/`aria-controls`/dynamic `aria-label`. Closed state uses `visibility` not `display` so the links stay out of the tab order AND the slide can animate. Closes on link click, Escape (returns focus to the toggle), outside click, and on leaving the breakpoint so a resize cannot strand it open on desktop. Verified: reachable nav actions go 3 to 8 on open, 0 page errors.
- 2026-07-25 COLOUR SCHEME INVERTED, and this supersedes the v2/v3 "true black base" decision. The operator raised colour three times; measuring settled it. Luminance sampled in ten bands down each page: palantir 61/154/126/222/248/240/196/243/216/251, nine of ten LIGHT. anduril three of ten. This page was ZERO of ten. Palantir is a light page with dark imagery as punctuation; the build was the exact inverse, and dark is also the category default an AI tool reaches for, which is part of why it read as less premium.
- The base is now white `#ffffff` with `#f4f3f0` alternating bands, ink `#14161a`, and `--plate: #05070a` for the bands that stay dark. The hero, the below-fold claim band, the console recording, the workspace mosaic and the archive band are the dark punctuation. Measured after: six of ten bands light. Not nine, because this page has more dark product imagery than palantir does, which is correct for what it sells.
- The archive band INVERTS with the scheme: it was the one light band on a dark page, it is now the one dark band on a light page. Same job, opposite polarity.
- Traps that cost real time in the flip: the nav needs TWO chrome treatments, light-on-dark over the hero and dark-on-light once scrolled, and a token swap alone leaves the wordmark, nav links and secondary CTA invisible. The inline logo SVGs hardcoded `#ecebe7`, so the mark stayed light on a white page until they were changed to `currentColor`; the mark now inherits `.brand` and follows both nav states for free. And four colours were dark-page-only: the amber domain qualifier measured 2.13:1 on white (now `#7a5e12`, 6.1:1), the evidence code chip 2.17:1, and both terminal comment colours 3.42 and 3.64.
- PRINCIPLE, learned the hard way over three oscillations: body copy over a photograph can only ever be subtle, because the scrim that makes it legible is the scrim that kills the image. The reference set never does it. A large headline goes on a photograph; body copy goes on a flat surface. The three plate-backed sections therefore became flat, and the sea plate was promoted to a full-bleed band carrying one line. What forced the realisation was measuring that the evidence chain's THIRD column sits in the bright half of its plate, so no left-weighted gradient could ever work there.
- NOT run for v2: the independent-judge portfolio test. The operator's standing rule in this session bans subagents, so the composite was graded by the builder, which is exactly the self-certification the pipeline warns about. Treat the parity claim as builder-graded, not judged.
- 2026-09-13 REBUILD as a sell, not a footage swap. Client: "the videos are repeatative, and low quality ... feel more like palantir.com", then "sell me this product. I am sure this isnt that good enough." Measured against palantir.com the same day: their hero reel is 60 s / 3840x2160 / ~60 shots at ~1 s, dark low-key real-world subjects with the product shown small on a laptop; ours was 12 s of five bright 1344x768 AI clips, and the SAME five clips were the carousel. Structure was already a Palantir clone; what did not sell was the copy (abstract headline, no buyer, no proof, limits above the CTA, "get started" = a repo link).
- Direction chosen by the client (G2 recorded against this session's transcript): real stock + product captures; hero headline to Archivo 400 (supersedes the July "Geist kept" note); product video per carousel tab + hover video per software row; full pitch rebuild for OSINT analysts and researchers.
- Hero footage is Pexels stock (licence: free use, no attribution required; credited in the footer anyway), sourced by `scripts/source-clips.mjs` from `scripts/reel.json`, cut by `scripts/build-reel.mjs` (the CUT table is the edit), one ffmpeg grade for all thirty sources. Four chrome-free product inserts. The hero aria-label says stock footage; nothing on the page calls stock "live".
- Product recordings come from `scripts/shoot-product.cjs`: frame-stepped 2560x1440 screenshots piped to ffmpeg from the running console (API :8000 + Vite dev :5173, DEV globals). While the NVIDIA driver is mismatched (reboot pending) Chrome renders on the AMD iGPU via `--use-angle=vulkan`; 0.35-0.8 s/frame, so an 8 s clip is about 90 s. Delivery encodes by `scripts/encode-product.sh`. Trap: hiding "notice" elements by walking up from a text match hid the whole app once (every frame black, identical file sizes); match only short leaf nodes.
- Numbers on the page were measured on the dev box 2026-09-13 04:55-04:58 UTC: 8,642 aircraft, 50,547 vessels, 45,414,961 archived positions in 9.25 GB since 2026-08-09, 85 MCP tools (`python -m app.mcp_server --list-tools`; the old "46" on the page and in docs/mcp-server.md was stale), and the agent panel transcript is a real `get_situation` call trimmed, not edited. Requirements tiers and the comparison table are the README's.
- Mobile overflow (147 px at 390, red for three rounds) was not the hero: it was the install grid (`1fr` columns growing to a `white-space:pre` block; fix `min-width:0`) and the outlined VELOCITY wordmark (`overflow:hidden`). Tap targets: dots, tabs, nav, footer links all lifted to 44 px.
- 2026-09-13 client, mid-build, three corrections that are now rules for this site: (1) a strip of big-number tiles with small captions plus a "measured on the dev box … counts drift" line is "the biggest AI tell sign"; numbers go inside sentences or not at all (the argument section's stats became two sentences, the hero proof line was cut entirely on the second pass: "like this is even worse. remove it"). (2) The 10px uppercase mono chip lines under carousel slides ("Recording · Starlink and stations") and the notes above the tabs: "remove all small texts like this". Slide tag + title stay. (3) Dense dark tables (the requirements tiers) are "very unreadable"; three plain lines with the hardware in white and the outcome under it. Also "Four commands. Then open localhost:8080." read as a gimmick; the install heading is now "Install on your own hardware."
- Same day: "a few repeat videos on the landing, remove them". Agent (launch film) and Workspace (console loop) tabs dropped: both are Europe-globe recordings a viewer cannot tell from Air. Seven tabs remain, each a different layer and a different place. Every mp4 in index.html is referenced exactly once (grep-checked).
- Hero contrast is gated by `scripts/hero-contrast.cjs` (pause per second, glyphs made transparent, brightest 2% per text box). Two instrument traps found while writing it: sampling at t=0 reads the poster, not the reel; and `.btn` has a 0.22 s colour transition, so the first sample after hiding the glyphs still contained them (read 2.39:1 on a button that measures 14:1). Set transition none before sampling. The 2,593-test line on the page is backed by a run this session: `2593 passed, 2 skipped in 173 s`.
- 2026-09-13 second pass, after the sell rebuild hit the session limit with three probes red (imagery 0.64/1000px, signature, proof). Fixes, all real captures: (1) the six software rows now show their still at every width, with the recording playing over it on hover; before this, touch and sub-960 viewers saw no evidence of the six apps at all, and the pitch critique had named it. Index moved into the text column because it sat on the still. (2) The carousel dots became a filmstrip of the seven tab stills with the same progress rule under the active one; shown on mobile as a scroll strip (the dots were hidden there). (3) The archive argument carries `hero-selected-track.jpg`, a police helicopter selected over Frankfurt with its dossier and recorded track (July 2026 capture, dated in the frame), and the install column carries `console-live.jpg`, the console with the layer rail open. Captions state the box and the month. (4) Hero sub split into two sentences so "Nothing deleted after 7 days" is its own short line: the proof probe needs a number with a unit in an element under 90 characters, and the nav GitHub button had no proof within 1200 px. Numbers stay inside sentences per the mid-build rule. (5) Signature declared as what the page already ran three times: the compose command in the hero button, the live agent transcript, the install block. No pixels added for it. (6) Limits moved BELOW the closing CTA band (the 7-point plan's item 6; position had not been ruled on, copy untouched). Register stays client-build: palantir.com measures ~5.8 images per 1000 px, so the 1.5 floor is below the reference, and the saas-product floor (1.1) would have lowered a bar the client raised. Result 20 rasters, 2.1/1000 px at 1440 and ~1.5 at 390. Thumbnails needed a 60% white border to clear the 3:1 control-boundary check against the dark bar. `.pwp-*` receipts added to `.assetsignore` so a deploy cannot publish them. The d7edab63 "upscale the AI clips after reboot" item is superseded: the reel is Pexels stock at 1920x1080 now, there is nothing to upscale.
- 2026-09-13 press.html was UNSTYLED in production since commit de6509f (the v3 cutover deleted `styles.css` and `main.js` as "orphaned"; press.html still links `styles.css`, and it is the page the homepage's two "Watch the tour" CTAs land on). Found by gating the site, not the page. `styles.css` restored from `de6509f^`; every url() it references (two fonts, two posters) still exists. Mobile table overflow (466 px at 390: nowrap headers plus the install command) fixed by letting the table scroll inside its own box. The press kit is an asset page, not a landing page: the conversion-band, signature, split-emphasis and hierarchy probes measure it against the homepage rubric and will stay red by design. Its receipt is kept in the scratchpad; the receipt on disk is the homepage's.
