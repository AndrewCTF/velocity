# Cryptography and key management

| Field | Value |
| --- | --- |
| Covers | OWASP ASVS 5.0 V11.1.1 (key management policy), V11.1.2 (cryptographic inventory); ISO/IEC 27001:2022 A.8.24 |
| Version | 1.0 (Draft until merged to `master`) |
| Adopted | 2026-09-13 |
| Owner | Maintainer (policy); Operator (keys of a deployment) |
| Code citations | `path:line` at commit `2cd38d9` on branch `compliance-2026-09`. Read them with `git show 2cd38d9:<path>`. Other work on this branch was changing `auth.py`, `config.py` and `keys.py` while this was written, so line numbers can drift after `2cd38d9` |

Every claim about behaviour cites the line that implements it. "Policy" marks a rule that this document
sets and the code does not enforce.

## 1. Key management policy (V11.1.1)

### 1.1 General rules (policy, adopted 2026-09-13)

1. Generate secrets with a CSPRNG: `secrets.token_urlsafe`, `openssl rand`, or `Fernet.generate_key()`.
   Never derive them from passwords or words.
2. Keep deployment secrets only in the operator's `.env`, or in a secret manager that injects environment
   variables. `.env` and `*.env` are gitignored (`.gitignore:11-13`). GitHub push protection is enabled on
   the repository ([`isms/statement-of-applicability.md`](isms/statement-of-applicability.md), 5.17).
3. Never log a secret, and never return one after the response that mints it.
4. Rotate a key at once, whatever its schedule, on any of these **trigger events**: suspected or confirmed
   leak; a person who knew the key loses their role (maintainer change, departing co-operator); the host,
   a backup or the `.env` is exposed; a dependency advisory affects the primitive. The full procedure is in
   [`incident-response.md`](incident-response.md) §3.
5. Destroy a key by removing it from `.env` and the secret manager, recreating the containers so no
   process still holds it, and deleting or re-keying any backup that contains the `.env`.

### 1.2 Per key

| Key | Purpose | Generation | Minimum length | Storage | Rotation cadence and triggers | Destruction |
| --- | --- | --- | --- | --- | --- | --- |
| `API_KEY` | Static shared secret for server, MCP and CI callers, and for single-user deployments | Operator runs `python -c "import secrets;print(secrets.token_urlsafe(32))"` (policy). Not generated by the app | Policy: 32 characters. **Not enforced at `2cd38d9`**; a startup check for 32 characters is being added on this branch and was uncommitted when this was written | `.env` (`.env.example:64`), loaded as `Settings.api_key` (`apps/api/app/config.py:245`). Compared in constant time (`apps/api/app/auth.py:197`). HTTP accepts it only in a header; `?key=` is accepted only on WebSocket upgrades (`apps/api/app/auth.py:15-16`, `apps/api/app/auth.py:255`). **If the web bundle is built with `VITE_API_KEY` (`.env.example:63`), the key is embedded in public JavaScript and is not a secret from anyone who can load the page** | Yearly (policy), and on every trigger in §1.1. Because one key is one principal (`docs/decisions.md`, 2026-09-13), rotating it logs every client out | Remove from `.env`; rebuild the web bundle if `VITE_API_KEY` was set; recreate containers |
| `SUPABASE_JWT_SECRET` | Verifies Supabase HS256 session JWTs locally; also signs the MCP internal token | Issued by the Supabase project (JWT settings) | Policy: 32 characters (Supabase's own lower bound). Not enforced at `2cd38d9`; startup check in progress as above | `.env`, `Settings.supabase_jwt_secret` (`apps/api/app/config.py:311`) | On every trigger. Rotating it in Supabase invalidates every session and the MCP token, which is re-minted on the next call (`apps/api/app/mcp_server.py:177-180`) | Rotate in Supabase; remove the old value from `.env` |
| `BYOK_ENC_KEY` (Fernet) | Encrypts users' own upstream API keys before they are stored in Supabase `public.user_keys` (`apps/api/app/keys.py:1-12`) | `Fernet.generate_key()`, documented at `apps/api/app/config.py:314-318` (32 random bytes, urlsafe base64) | Fixed by Fernet: 32 bytes. A malformed key makes the BYOK routes return 503 (`apps/api/app/keys.py:101-104`) | `.env`, `Settings.byok_enc_key` (`apps/api/app/config.py:319`). Ciphertext only in the database (`apps/api/app/keys.py:10-12`); the GET route returns a masked hint (`apps/api/app/keys.py:5-6`) | Yearly (policy), and on every trigger. **Procedure: prepend the new key, re-encrypt, remove the old.** Set the new key first in the key list so it encrypts, keep the old key second so existing rows still decrypt, re-encrypt every row, then remove the old key. **At `2cd38d9` the code builds a single `Fernet` (`apps/api/app/keys.py:96-104`), so a key list is not yet accepted.** `MultiFernet` support is being added to `apps/api/app/keys.py` on this branch. Until it lands, rotation is destructive: stored keys become unreadable (`decrypt_value` returns `None`, `apps/api/app/keys.py:112-117`) and users re-enter them, as `incident-response.md` §3 states | Remove from `.env` after the re-encryption, or after users have re-entered their keys |
| Foundry ingest tokens | Bearer credential for `POST /api/ingest/{dataset_id}` (`apps/api/app/routes/ingest.py:16-26`) | Server-side, `secrets.token_urlsafe(32)` (`apps/api/app/foundry/store.py:330`): 32 random bytes | 43 characters, set by the generator | Only the SHA-256 is stored (`apps/api/app/foundry/store.py:195-196`, `apps/api/app/foundry/store.py:336-338`); plaintext is shown once (`apps/api/app/foundry/store.py:322-324`); verified with `compare_digest` (`apps/api/app/foundry/store.py:379`) | On every trigger, and when a sender is decommissioned. Re-minting replaces the previous token (`apps/api/app/foundry/store.py:327-329`) | Re-mint, or delete the dataset (`apps/api/app/foundry/store.py:416-422`) |
| MCP internal token | Short-lived HS256 JWT that the MCP server mints for its own calls to `/api/intel/*` when no static key is set (`apps/api/app/mcp_server.py:206-213`) | Minted in process with PyJWT from `SUPABASE_JWT_SECRET` (`apps/api/app/mcp_server.py:182-194`) | Inherits the JWT secret | Memory only, cached until 60 s before expiry (`apps/api/app/mcp_server.py:160-163`, `apps/api/app/mcp_server.py:177-180`) | Automatic: 10-minute lifetime (`apps/api/app/mcp_server.py:162`). Rotating the JWT secret invalidates it | Expires; process restart clears the cache |
| Local model sidecar keys | Bearer key between the API and the llama.cpp or vLLM sidecar on loopback | `secrets.token_urlsafe(32)` per boot (`apps/api/app/llamacpp_sidecar.py:9`, `apps/api/app/llamacpp_sidecar.py:289`, `apps/api/app/vllm_sidecar.py:155`) | 43 characters | Process memory | Every boot | Process exit |
| TLS certificate and private key | HTTPS for a deployment | The operator's host reverse proxy (for example ACME) | Policy: ECDSA P-256 or RSA 2048 or larger; TLS 1.2 minimum | On the host proxy. **Not in this repository:** nginx listens on plain HTTP bound to `127.0.0.1:8080` (`docker-compose.prod.yml:98`) and the prod nginx config has no `ssl_certificate` | The proxy's automatic renewal; revoke and reissue on key exposure | Revoke with the CA; delete from the host |
| Maintainer commit signing key | SSH signature on commits, required by the repository ruleset | `ssh-keygen -t ed25519`. The local signing key is `ssh-ed25519`, and git is configured with `gpg.format=ssh` and `commit.gpgsign=true` (read with `git config` on 2026-09-13) | Ed25519 (fixed size) | Maintainer endpoint (`~/.ssh`); public half registered on GitHub. Recent branch commits `2c7a6b7`, `bbc1aae` and `2cd38d9` verify as `G` (`git log --format=%G?`) | Yearly (policy), and on endpoint loss or compromise | Remove from GitHub signing keys; delete the private key; see RA-08 (endpoint baseline) |

## 2. Cryptographic inventory (V11.1.2)

Libraries resolved in `apps/api/uv.lock` at `2cd38d9`: `cryptography` 50.0.1, `httpx` 0.28.1,
`certifi` 2026.7.22, `pyjwt` 2.14.0. Declared in `apps/api/pyproject.toml:24`, `apps/api/pyproject.toml:43-44`.

| Algorithm | Library | Purpose | Security role | Location |
| --- | --- | --- | --- | --- |
| HMAC-SHA256 (JWT HS256 verify) | stdlib `hmac`, `hashlib` (hand-written) | Verify Supabase session JWTs; checks `exp`, `nbf`, `aud` and `role` | Authentication | `apps/api/app/auth.py:106-130` |
| HMAC-SHA256 (JWT HS256 sign) | PyJWT | Mint the MCP internal token | Authentication | `apps/api/app/mcp_server.py:182-194` |
| Constant-time comparison | stdlib `hmac.compare_digest` / `secrets.compare_digest` | JWT signature, static key, ingest token hash | Timing-attack resistance | `apps/api/app/auth.py:112`, `apps/api/app/auth.py:197`, `apps/api/app/foundry/store.py:379` |
| Fernet (AES-128-CBC, PKCS7, HMAC-SHA256, versioned token) | `cryptography.fernet` | Encrypt user BYOK keys at rest | Confidentiality and integrity | `apps/api/app/keys.py:24`, `apps/api/app/keys.py:96-117` |
| SHA-256 | stdlib `hashlib` | Ingest token storage (the token has 256 bits of entropy, so no salt or KDF is needed) | Credential storage | `apps/api/app/foundry/store.py:195-196` |
| SHA-256 | stdlib `hashlib` | Evidence content addressing, and a manifest hash over the member hashes | Integrity, chain of custody | `apps/api/app/intel/evidence.py:99`, `apps/api/app/intel/evidence.py:719`; re-verified before serving (`apps/api/app/routes/evidence.py:330-331`) |
| SHA-256 | stdlib `hashlib` | Verify downloaded llama.cpp binaries and model files | Integrity of fetched executables | `apps/api/app/localllm/binary.py:175`, `apps/api/app/localllm/manager.py:462` |
| CSPRNG | stdlib `secrets` | Ingest tokens and sidecar keys | Key generation | `apps/api/app/foundry/store.py:330`, `apps/api/app/llamacpp_sidecar.py:289`, `apps/api/app/vllm_sidecar.py:155` |
| TLS (system OpenSSL; CA bundle from certifi) | `httpx` default verification | All outbound HTTPS to upstreams through the shared client. No `verify=False` in `apps/api/app` (`git grep` on `2cd38d9`) | Transport confidentiality and server authentication | `apps/api/app/upstream.py:344-349` |
| TLS (inbound) | Operator's host proxy | Client-to-deployment HTTPS | Transport | Outside the repository (`docker-compose.prod.yml:98`) |
| SSH Ed25519 signatures | OpenSSH through git | Commit signing | Source integrity | Maintainer git configuration (§1.2) |
| MD5 | stdlib `hashlib` | Gravatar and Libravatar lookup keys, which those services require | **Not a security use** | `apps/api/app/osint/connectors.py:262`, `apps/api/app/osint/sources/social.py:77` |
| MD5 | stdlib `hashlib` | Cache keys and ETags | **Not a security use** | `apps/api/app/routes/adsb.py:536`, `apps/api/app/routes/adsb.py:2314`, `apps/api/app/routes/adsb.py:2332`, `apps/api/app/routes/maritime.py:410`, `apps/api/app/news/analyze.py:820`, `apps/api/app/places.py:191` |
| SHA-1 (truncated) | stdlib `hashlib` | Stable short identifiers | **Not a security use** | `apps/api/app/intel/promotion.py:55`, `apps/api/app/localllm/manager.py:62`, `apps/api/app/routes/ai_selection.py:377` |
| SHA-256 (truncated to 16 hex) | stdlib `hashlib` | Fetch cache key | **Not a security use** | `apps/api/app/osint/fetch.py:354` |
| SHA-256 key derivation for Apple Maps tokens | stdlib `hashlib` | Reproduces a third-party client's token scheme | Interoperability, not protection of project data | `apps/api/app/apple_maps.py:122`, `tools/apple-flyover/apple3d/auth.py:26` |

Web client: the Supabase session is kept in `localStorage` by supabase-js (`apps/web/src/transport/supabase.ts:13`,
`apps/web/src/transport/supabase.ts:35`). No application-level cryptography runs in the browser.

## 3. Deprecation and post-quantum note

- **Hand-written HS256.** `_verify_hs256` (`apps/api/app/auth.py:106-130`) re-implements verification that
  PyJWT, already a dependency (`apps/api/pyproject.toml:44`), provides. It is correct for the checks it
  makes, but a library path would also reject `alg` confusion and malformed headers by construction.
  Review it at the next ASVS cycle.
- **Symmetric JWTs.** HS256 means every verifier can also mint tokens (the MCP server does, at
  `apps/api/app/mcp_server.py:166-198`). Supabase's asymmetric signing keys (ES256 or RS256 through JWKS)
  would remove that property; at `2cd38d9` `apps/api/app/auth.py` has no JWKS path. Tracked as a
  candidate improvement, not a defect.
- **Fernet.** It uses AES-128-CBC with HMAC-SHA256. It is not deprecated, and 128-bit AES keeps an
  adequate margin against Grover-type quantum search for data at rest. If the project moves to an AEAD
  (AES-256-GCM or ChaCha20-Poly1305), the prepend, re-encrypt, remove procedure in §1.2 carries the
  migration.
- **MD5 and SHA-1** are used only as non-security identifiers (§2). They must never be used for integrity or
  credentials; CodeQL flags new uses (`.github/workflows/codeql.yml`).
- **Post-quantum.** Everything here that a quantum computer threatens is **asymmetric**: TLS key exchange
  and certificates at the operator's proxy, and Ed25519 commit signatures. The symmetric primitives
  (HMAC-SHA256, SHA-256, AES-128) are not broken by Shor's algorithm. Actions: (a) operators should enable
  hybrid post-quantum key exchange (X25519MLKEM768) on their TLS proxy when their proxy supports it, which
  protects captured traffic against later decryption; (b) review commit signing when git and GitHub support
  a post-quantum signature scheme. The project controls neither schedule. This note is reviewed yearly with
  the SoA.
